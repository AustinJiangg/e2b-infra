# e2b-deploy/ 是 e2b-deploy.tar.gz 的「可读」目录。
# 整个目录是要进仓库的，但其中两类内容刻意不跟踪：

# 1) 约 70MB 的二进制大文件，没必要进仓库（仍保留在 e2b-deploy.tar.gz 内供构建使用）
e2b-deploy/dep/ubuntu-22.04-custom.tar.gz

# 2) 压缩包内自带的 git 仓库：二进制且无法 diff，保留还会让 e2b-deploy/ 被当成子模块
e2b-deploy/.git/

# 3) rollback/slides/ 是 slidev 工程，node_modules 有 560+ MB。
# 该目录自己有一份 .gitignore，但那只在目录被跟踪时生效——目前它整个未跟踪，
# 一次 `git add -A` 就会把 node_modules 提交进来，push 上去基本救不回来。
# 这里再兜一道，与目录内那份重复是有意的。
# 不带尾斜杠：docs worktree 里这一项是指回主工作区的软链接，带斜杠只匹配目录会漏掉它。
rollback/slides/node_modules
rollback/slides/dist/
rollback/slides/.slidev/
rollback/slides/.local-libs/

# 4) rollback/dist/ 是 rollback/docs/ 的构建产物（单文件 HTML + zip + tar.gz）。
# 每次重新生成都会产生一个几 MB 的全新二进制 blob，进 git 只会让仓库越来越胖。
# 分发走 GitHub Release，源在 rollback/docs/。
rollback/dist/
