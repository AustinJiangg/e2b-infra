# 28 · 实测结果与达标判定

> 全书唯一放实测数字的一篇。前面四篇讲的是**怎么测**，这一篇只回答**测出来是多少、
> 对照客户的两个指标达没达标**。所有数字都带着它的条件标签；不带标签的数字在本书里
> 不允许被引用，也不允许跨机器相减。
>
> **读者**：评审、客户、要复测的人、接手基准的工程师。
> **预备**：[第 24 篇 §4 · 测量纪律](24-test-overview.md#4-测量纪律)、
> [第 26 篇 §1 · 指标口径](26-performance-methodology.md#1-指标口径)。
> **数据**：[`../scripts/dev/reports/`](../scripts/dev/reports/)、
> [`../scripts/dev/耗时基准结论.md`](../scripts/dev/耗时基准结论.md)、
> `benchmark/checkpoint-bench-对比.md`（主工作区，未提交）。

---

## 0. 本篇要回答的问题

1. 两台机器的条件差在哪，为什么它们的数字只能并列、不能相减？
2. 功能正确性到今天为止证到了什么程度？哪几轮的 `mem_mode` 判据是有效的？
3. 增量 checkpoint 与原地 restore 对照「≤ 200 ms / ≤ 100 ms」达标了吗？哪些格子没达标？
4. 950 上还缺哪些数据，缺的那些分别要跑哪个脚本才能补上？
5. 已知的劣化有哪些，对使用场景意味着什么？

**单位约定**：原始报告以秒记时，本篇统一换算成毫秒（超过 1 秒的仍写秒）。
容量沿用原始报告的 MB / GB 写法。

---

## 1. 条件标签

### 1.1 两台机器差在哪

| | **950**（`slot6`） | **920B** |
|---|---|---|
| CPU | Kunpeng 950 7592C，384 线程 | Kunpeng 920 7270Z，128 核 |
| MIDR part | `0xd06` | `0xd02` |
| 内核 | 6.6.0-159.4.3.154.oe2403sp4 | 6.6.0-28.0.0.34.oe2403 |
| **HDBSS 硬件标脏** | **有**（`KVM_ENABLE_CAP(502)` 真开成功） | **没有** —— 内核里压根没编 `CONFIG_ARM64_HDBSS` |
| 实际脏页后端 | `hdbss` | `kvm-wp`（软件写保护） |
| 根盘文件系统 | **ext4**，988 G | **XFS**，3.0 T |
| 产物落在哪 | 根盘 **真盘 ext4** | 视轮次：调优过的 **loop** 卷（ext4 / XFS），或根盘真盘 XFS |
| 模板 | `base`（内存规格那一轮**没有记录**） | `base`，2 vCPU / 2048 MB |
| 二进制 | Firecracker 1.12.1；orchestrator sha **未记录** | 见 §1.2 逐轮 |

硬件与 HDBSS 三层证据的完整版在[第 19 篇 §4.1](19-kunpeng-platform.md#41-硬件与系统)与
[§4.2](19-kunpeng-platform.md#42-hdbss-的三层证据)。

### 1.2 为什么这两台机器的数字不能相减

差的不是一项，是**四项同时在差**：脏页后端（硬件标脏 vs 软件写保护）、
产物介质（真盘 vs loop 卷）、产物文件系统、模板内存规格。

一个具体例子：950 的 `checkpoint_verify.py` 那轮全量 0.283 s，920B 09-04 同一个脚本
同样参数是 2.108 s，差 7.4 倍。这 7.4 倍里至少混着「HDBSS 省掉的 VM exit」
「真盘 vs loop」「模板内存规格可能不同」三笔，**没有办法把其中一笔单独摘出来**。
写成「HDBSS 带来 7 倍收益」是错的
（[第 24 篇 §4.5](24-test-overview.md#45-不同条件的数不相减)）。

要量化 HDBSS 的收益，唯一正确的做法是**在同一台 950 上跑两遍**——
一遍走 HDBSS、一遍强制走软件写保护，其余条件全不动。这一项在 §8 里列为缺口。

### 1.3 每一轮的身份证

下表是本篇引用的全部轮次。后面每张表都注明它出自哪一轮，读表时回到这里查条件。

| 轮次 | 日期 | 机器 · 后端 | 方案 | 产物文件系统 · 介质 | 二进制 | 位置 |
|---|---|---|---|---|---|---|
| **950-verify** | 2026-08-29 | 950 · `hdbss` | ext4 | ext4 · **真盘**（根盘） | FC 1.12.1，orchestrator 未记录 | [`reports/950-verify-20260829/`](../scripts/dev/reports/950-verify-20260829/) |
| **920B-0824-fn** | 2026-08-24 | 920B · `kvm-wp` | ext4 / XFS 各一轮 | ext4 · loop（`/mnt/ext4dev`）；XFS · 真盘（`/`） | 未记录 | `reports/ext4-20260824-161727/`、`reports/xfs-20260824-161025/` |
| **920B-0824-bench** | 2026-08-24 | 920B · `kvm-wp` | ext4 / XFS 各一轮 | 同上 | 未记录 | [`reports/bench-ext4-20260824-200601/`](../scripts/dev/reports/bench-ext4-20260824-200601/)、`reports/bench-xfs-20260824-201348/` |
| **920B-0824-ramp** | 2026-08-24 | 920B · `kvm-wp` | ext4 | ext4 · loop | 未记录 | `reports/ramp-ext4-20260824-215331/` |
| **920B-0825-ext4** | 2026-08-25 | 920B · `kvm-wp` | ext4 | ext4 · 300 G loop（`direct-io` / 预分配 / 4K 扇区 / `nodiscard`） | 未记录 | `reports/ext4-20260825-154832/` |
| **920B-0825-xfs** | 2026-08-25 | 920B · `kvm-wp` | XFS | XFS · 同参数 300 G loop，`cowextsize=4096` | 未记录 | `reports/xfs-cow4k/`（未调 `cowextsize` 的同轮在 `reports/xfs-20260825-162413/`） |
| **920B-0904** | 2026-09-04 | 920B · `kvm-wp` | ext4 | **XFS** · 真盘 | orchestrator `aa13d05e9a…`、firecracker `05774e5376…`（构建自 KASandbox_0904 —— 即上游 MR !119 的源分支，这一轮验的就是合入 openEuler 的代码） | `e2b-infra/rollback/scripts/dev/reports/920b-kas0904-20260904/` |
| **920B-0908** | 2026-09-08 | 920B · `kvm-wp` | ext4 | XFS · 真盘 | `orchestrator-ext4-ecdad325c-pausefix` | 原始日志在 `deltabox/e2b/results/ckpt-920b-20260908/`，**本仓库未收** |
| **950-native** | 2026-09-09 ~ 10 | 950 · 原生 pause 路径 | —— | 导出缓存 XFS on LVM | —— | `三套快照方案的负载构造与测量口径.md` §八 |
| **920B-0914-native4k** | 2026-09-14 | 920B · 原生路径，修复后（`de25fe4d0`）· 软件写保护 | 9bfa37ab | 导出缓存 ext4 loop（direct-io），模板存储 tmpfs | —— | 920B `tmp/native4k-bench-920b-de25fe4d0.log`；`原生快照精确增量-方案设计.md` §8 |

> 「二进制未记录」不是疏忽的托词，是**如实记录**：那几轮没有把 `sha256sum` 抄进
> `00-context.txt`。按[第 24 篇 §4.2](24-test-overview.md#42-一个数字要带的条件标签)，
> 这一列本该有；补不上就空着，不猜
> （[第 24 篇 §4.4](24-test-overview.md#44-空着比错的数强)）。

---

## 2. 功能正确性结果

### 2.1 总表

「判据有效」这一列指的是 `mem_mode` 那两条断言 —— SDK 不透出该字段时脚本会**跳过**它们，
59 项静默变成 57 项（[第 25 篇 §2.4](25-functional-tests.md#24-mem_mode-断言以及它依赖哪一版-sdk)）。

| 日期 | 机器 | 脚本 | SDK | `mem_mode` 判据 | 结果 |
|---|---|---|---|---|---|
| 08-24 | 920B | `correctness.py`（ext4 方案） | 交付覆盖层 | **有效** | **31/31 ALL PASS** |
| 08-24 | 920B | `timing.py ext4 /` | 交付覆盖层 | 有效 | **ALL CORRECT** |
| 08-24 | 920B | `loop.py ext4 200` | —— | 不涉及 | **STABLE**，200 次 `failures: 0` |
| 08-24 | 920B | 同上三件套（XFS 方案） | 交付覆盖层 | 有效 | **ALL PASS / ALL CORRECT / STABLE** |
| 08-25 | 920B | `correctness.py`（ext4 方案） | 交付覆盖层 | 有效（前 12 项已过） | **未跑完** —— 第 3 节之后客户端 `httpcore.ReadTimeout` 中断 |
| 08-25 | 920B | `timing.py` / `loop.py`（ext4 方案） | 交付覆盖层 | 有效 | **ALL CORRECT** / **STABLE** 200 次 0 失败 |
| 08-25 | 920B | 三件套（XFS 方案） | 交付覆盖层 | 有效 | **ALL PASS / ALL CORRECT / STABLE** |
| **08-29** | **950** | **`checkpoint_verify.py`** | 交付覆盖层 | **有效**（`full` / `incremental` / `incremental`） | **59/59 全部通过** |
| 09-04 | 920B | `checkpoint_verify.py`（`ours-verify.log`） | **交付的 py-sdk 覆盖层** | **有效** | **59/59 ALL PASS** |
| 09-04 | 920B | `checkpoint_verify.py`（`e2e-verify.log`） | 旧 gsd SDK | **失效**，三代全报 `mem_mode=?` | 57/57 ALL PASS |
| 09-04 | 920B | `checkpoint_verify.py`（`kas0904-verify.log`，原 infra-arm 二进制对照） | 旧 gsd SDK | **失效** | 57/57 ALL PASS |
| 09-04 | 920B | `correctness.py` | 交付覆盖层 | 有效 | **31/31 ALL PASS** |
| 09-04 | 920B | `timing.py ext4 /` | 交付覆盖层 | 有效 | **ALL CORRECT** |
| 09-04 | 920B | `loop.py ext4 200` | 交付覆盖层 | 不涉及 | **STABLE**，200 次 0 失败，p50 48 ms / max 402 ms |
| 09-04 | 920B | `pause_verify.py`（`t-pause.log` 旧 SDK / `ours-pause.log` 交付 SDK） | 两版各一轮 | 不涉及 | **两轮都 PASS**（见 §2.3） |
| 09-04 | 920B | `compat_matrix.py` | 交付覆盖层 | 不涉及 | **OK 13 / REFUSED 2 / BROKEN 0** |
| 09-08 | 920B | `checkpoint_verify.py` 与两个基准脚本 | 基准环境里那版，**不带 `mem_mode`** | **失效**，三个脚本全报 `?` | 判据空转；旁证顶上（§2.2） |
| —— | 950 | `correctness.py` / `pause_verify.py` / `compat_matrix.py` / `loop.py` | —— | —— | **待测** |

三条读法：

- **57 和 59 都写着「全部通过」**，差别只有把两份日志并排才看得出来 —— 57 那两轮里，
  防「增量静默退化成全量」的判据是空的。09-04 三轮日志是
  [第 24 篇 §1.4](24-test-overview.md#14-判据本身静默失效) 那种静默失效的现成对照组。
- **950 那一轮是本方案第一次在硬件标脏路径上得到验证**：服务端自报
  `脏页后端 = HDBSS（硬件标脏）`，三代 `mem_mode` 是 `full` / `incremental` / `incremental`。
- 950 上的树语义、pause、兼容矩阵、稳定性四项**尚未跑过**。正确性结论可以从 920B 外推
  （两种脏页后端喂给上层的是同一张位图），但外推不等于测过，
  矩阵里那四格照旧写待测（[第 24 篇 §3](24-test-overview.md#3-测试矩阵)）。

> **关于「52 项」**：`../scripts/dev/MANIFEST.md` 里记着 920B 早期一轮 `checkpoint_verify.py`
> 的 **52/52**（当时脚本是「三代 × 11 个观测点」的版本），但那份文档自己也写明
> **「52/52 与 60/60 只存在于 git commit message 里」**——没有日志留档，所以本篇不收。
> 另一条线索是现行脚本在**模板里没有 `python3`** 时活体检查降为 4 项，总数同样落到 52
> （[第 25 篇 §2.5](25-functional-tests.md#25-59-项怎么构成)）。两种来源都能得到 52，
> **哪一个对应 MANIFEST 里那一轮无法判定**，两说并存记在这里。
> 同一段里的 `sweep 60/60` 在任何脚本里都找不到对应字面量，**不要引用**。

### 2.2 判据失效时靠什么顶

09-08 那一轮三个脚本全报 `mem_mode=?`。当时用的旁证是**产物实测**：
对照组脚本的内存产物列里，第一代是 **2048 MB**（整份 guest RAM），第七代只有 **276.5 MB**——
增量确实生效了（`benchmark/checkpoint-bench-对比.md` §5.1）。

这条旁证成立，但它是**人看出来的**，不是脚本判的。判据该修的方向也在那份文档里：
把断言从「服务端自报的 `mem_mode`」换成「内存产物是否接近 guest RAM 总量」，
宿主机上一个 `st_blocks` 就够，不依赖服务端与 SDK 的任何改动。

### 2.3 pause 与兼容矩阵

`pause_verify.py` 09-04 跑了两轮（旧 SDK 一轮、交付 SDK 一轮），**都通过**：

```
before.bin  写入 510c2dceeccc7126  读回 510c2dceeccc7126
after.bin   写入 153060e6779190cd  读回 153060e6779190cd
  [PASS] **checkpoint 之前**写的 16MB 在 pause/resume 后完好   ← 这条抓 bug
  [PASS] checkpoint 之后写的 16MB 在 pause/resume 后完好
```

`O_DIRECT` 是这两条断言成立的前提，理由在
[第 25 篇 §4](25-functional-tests.md#4-pause_verifypy一个自洽的静默损坏)。

> **两份 pause 日志末尾的 traceback 要正确解读。** `ours-pause.log` / `t-pause.log`
> 都以一个 `missing header` 的 Python traceback 结束。那是脚本**旧版第 6 节**
> （pause/resume 之后再 restore）直接调 API 抛出来的，**不是前五节的断言失败**——
> 前五节两条 `[PASS]` 已经打完。追下去发现是 SDK `connect()` 路径没带
> `E2b-Sandbox-Id` / `E2b-Sandbox-Port` 两个头；补上之后错误变成
> `not found: checkpoint ckpt_… not found`（`ours-pause2.log`），说明**服务端是好的**，
> 而 pause/resume 之后 checkpoint 账本确实不再可用。
> **交付版脚本已经把第 6 节改写成边界断言**（预期被拒绝而不是预期成功），
> 所以现在这条是一条「行为边界」而不是一条失败。

`compat_matrix.py` 的两个 `REFUSED` 与上面是同一件事的两面：

| 项 | 结果 | 说明 |
|---|---|---|
| B4 `pause → connect` 之后 restore 旧 checkpoint | **REFUSED** | pause/resume 之后账本不再可用。**拒绝而不是拿失效层栈去恢复**，属于边界不是缺陷 |
| F1 `kill` 之后 connect | **REFUSED** | 沙箱已删除，原生本来就不允许 |
| 其余 13 项（含 B7「pause 之后新建的 checkpoint 能正常 restore」、E1「pause → connect → checkpoint → restore」） | **OK** | 原生生命周期能力没有被 checkpoint / restore 破坏 |

边界的完整讨论见[第 16 篇](16-lifecycle-and-portability.md)与
[第 25 篇 §5](25-functional-tests.md#5-compat_matrixpy与原生生命周期的组合矩阵)。

---

## 3. 性能 · 950（HDBSS，真盘 ext4）

**这一节大部分是空的，而且会一直空到分档基准在 950 上跑完为止。**
骨架先立在这里，格子写「待测」，是为了让缺口是**可数的**：
下面每张表的表头都注明了要跑哪个脚本、跑完哪一列会被填上。

### 3.1 唯一有数的一轮：`checkpoint_verify.py` 顺带的墙钟

**条件**：950-verify 轮 —— 950 · `hdbss` · 真盘 ext4 · 模板 `base` ·
每代脏内存 128 MB + 根文件系统 32 MB · 2026-08-29。

| 操作 | n | p50 | min | max |
|---|---|---|---|---|
| create 全量（第一代） | 1 | 283 ms | 283 ms | 283 ms |
| create 增量（gB / gC） | 2 | 113 ms | 111 ms | 115 ms |
| restore | 10 | 100 ms | 89 ms | 153 ms |

> **这不是性能基准。** `checkpoint_verify.py` 的定位是正确性验收，耗时只是顺带打印；
> 样本量 n = 1 / 2 / 10，不构成 [第 26 篇 §1.2](26-performance-methodology.md#12-口径定义表)
> 要求的分布；负载也不是「预热 + 原地覆写」造出来的档位负载。
> 拿它当量级看可以，拿它填判定表不行 —— 逐条理由见 §5.4。

### 3.2 分档基准骨架【待测】

#### 表 3-A · `checkpoint_bench.py`：时机成本与产物实测

一条链、两遍拍，① 减 ② 就是「写完立刻拍」的时机成本
（[第 26 篇 §3.2](26-performance-methodology.md#32-两遍拍①-减-②-就是时机成本)）。

| 名义 内存/文件 | ① 立刻拍 e2e | ② 冲干净再拍 e2e | 时机成本 ①−② | 实测内存产物 | 实测文件产物 |
|---|---|---|---|---|---|
| 全量（树根） | 待测 | 待测 | —— | 待测 | 待测 |
| 0 / 0 | 待测 | 待测 | 待测 | 待测 | 待测 |
| 12 / 4 | 待测 | 待测 | 待测 | 待测 | 待测 |
| 24 / 8 | 待测 | 待测 | 待测 | 待测 | 待测 |
| 48 / 16 | 待测 | 待测 | 待测 | 待测 | 待测 |
| 96 / 32 | 待测 | 待测 | 待测 | 待测 | 待测 |
| 192 / 64 | 待测 | 待测 | 待测 | 待测 | 待测 |

#### 表 3-B · `bench-ckpt.py`：档位分布（判定表由它产出）

| 脏页档 | create p50 | p99 | 新增物理空间 | `mem_mode` |
|---|---|---|---|---|
| 全量 | 待测 | 待测 | 待测 | 应为 `full` |
| 0 MB | 待测 | 待测 | 待测 | 待测 |
| 16 MB | 待测 | 待测 | 待测 | 待测 |
| 32 MB | 待测 | 待测 | 待测 | 待测 |
| 64 MB | 待测 | 待测 | 待测 | 待测 |
| 128 MB | 待测 | 待测 | 待测 | 待测 |
| 256 MB | 待测 | 待测 | 待测 | 待测 |

#### 表 3-C · `bench-ckpt.py` + `freeze_probe.py`：冻结窗口

HDBSS 的收益预期最大的就是这一张 —— 软件写保护的 VM exit 正好发生在暂停窗口里。

| 脏页档 | 冻结 p50 | 占墙钟 | 底噪 / 可区分 |
|---|---|---|---|
| 0 MB | 待测 | 待测 | 待测 |
| 16 MB | 待测 | 待测 | 待测 |
| 32 MB | 待测 | 待测 | 待测 |
| 64 MB | 待测 | 待测 | 待测 |
| 128 MB | 待测 | 待测 | 待测 |
| 256 MB | 待测 | 待测 | 待测 |

#### 表 3-D · `bench-ckpt.py`：restore 浅集与深集

| 脏页档 | 浅集 p50 | 浅集 p99 | 深集 p50（33 步） |
|---|---|---|---|
| 0 MB | 待测 | 待测 | 待测 |
| 16 MB | 待测 | 待测 | 待测 |
| 32 MB | 待测 | 待测 | 待测 |
| 64 MB | 待测 | 待测 | 待测 |
| 128 MB | 待测 | 待测 | 待测 |
| 256 MB | 待测 | 待测 | 待测 |

#### 表 3-E · `bench-ckpt.py`：60 秒持续连打

| 次数 | 速率 | p50 | p99 | 前 1/3 → 后 1/3 |
|---|---|---|---|---|
| 待测 | 待测 | 待测 | 待测 | 待测 |

#### 表 3-F · `timing.py`：链深对照

| 链深 | create p50 | 深集回退 | 深集前滚 | 浅集 p50 |
|---|---|---|---|---|
| 5 | 待测 | 待测 | 待测 | 待测 |
| 20 | 待测 | 待测 | 待测 | 待测 |
| 50 | 待测 | 待测 | 待测 | 待测 |

#### 表 3-G · `loop.py`：稳定性

| 次数 | failures | p50 / p90 | max |
|---|---|---|---|
| 待测 | 待测 | 待测 | 待测 |

### 3.3 跨实现对照在 950 上的状态

#### 表 3-H · `checkpoint_bench_v2.py`（对照组脚本）【待测】

| 档 名义 内存/文件 | e2e | 内存产物 |
|---|---|---|
| cp0 全量 | 待测 | 待测 |
| cp1 0 / 0 | 待测 | 待测 |
| cp2 12 / 4 | 待测 | 待测 |
| cp3 24 / 8 | 待测 | 待测 |
| cp4 48 / 16 | 待测 | 待测 |
| cp5 96 / 32 | 待测 | 待测 |
| cp6 192 / 64 | 待测 | 待测 |

> 950 上跑过一轮**负载重写之前**的同族脚本，那一轮唯一留下来的结论是负载放大：
> 档位标 256 MB（名义内存份 192 MB），量到的内存差分 **1087 MB** —— 对名义内存份是
> 5.7 倍、对档位标签是 4.2 倍。那批耗时数字因为档位标签名不副实而**不可用**，
> 所以这张表整张待测。放大是怎么来的见
> [第 26 篇 §2.2](26-performance-methodology.md#22-失真二写放大)。

#### 表 3-I · `native_snapshot_bench.py`：e2b 原生 snapshot（**已有数据**）

**条件**：950-native 轮 —— 950 · 原生 pause 路径 · 2026-09-09 ~ 10 ·
负载按「预热 + 原地覆写」构造，档位与另两套逐格相同。

| 档 | 名义脏内存 | 导出的 memfile |
|---|---|---|
| cp0 | 0 MB | 406 MB |
| cp1 | 12 MB | 394 MB |
| cp2 | 24 MB | 402 MB |
| cp3 | 48 MB | 398 MB |
| cp4 | 96 MB | 404 MB |
| cp5 | 192 MB | 410 MB |

**这张表最值得看的不是时间，是那一列几乎不动的产物量。** 名义值从 0 长到 192 MB，
导出量一直在 400 上下 —— 原生在 ARM 这条线上的「脏页集」实际上是「自上次 resume 以来
被碰过的页集」，也就是工作集。链条与代码位置（`fbee6fcd1` 注释掉了 `UFFDIO_COPY_MODE_WP`）在
[第 27 篇 §5](27-cross-implementation.md#5-一个真实发现读也被算成脏)。
本方案不用这个判据，所以同一台机器上本方案的内存差分是随档位线性增长的。

同一轮还量到两个数，读原生的恢复时间时必须一起看：**resume 端到端约 90 ms，
恢复之后第一次把负载整读一遍额外要 165 ms**（懒加载延后付的账）。
[第 27 篇 §4.5](27-cross-implementation.md#45-哪些列可以并排) 规定：
两张表并排时必须把 `touch` 的差值加到原生那一边，否则是拿「还没付的账」
跟「已经付了的账」比。

#### 表 3-J · `native_snapshot_bench.py`（修复后）：920B-0914-native4k

**条件**：920B · 原生路径修复后（infra-arm `jll` `de25fe4d0`）· `FC_TRACK_DIRTY_PAGES=true`（软件写保护）·
同一份脚本、同一档位表；「修复前」列是同机同 FC 前一轮（09-11）。cp0 含预热写入的 192 + 64 MB。

| 档 | 名义脏内存 / 文件 | memfile 修复前 | memfile 修复后 | pause ms | touch1 ms 修复前 → 后 |
|---|---|---|---|---|---|
| cp0 | 0 / 0 | 442 | 282 | 519 | 210 → 264 |
| cp1 | 12 / 4 | 432 | **38** | 260 | 258 → 207 |
| cp2 | 24 / 8 | 434 | **54** | 289 | 217 → 215 |
| cp3 | 48 / 16 | 440 | **88** | 309 | 248 → 202 |
| cp4 | 96 / 32 | 442 | **153** | 496 | 303 → 203 |
| cp5 | 192 / 64 | 450 | **288** | 684 | 223 → 230 |

snapshot 模式（每代持久模板、另起新沙箱恢复）memfile 同上；create_snapshot 端到端 0.6 ~ 1.8 s、
restore 端到端 0.2 ~ 1.3 s，修复前后同一噪声带；restore 后 `touch1` 241 ~ 266 ms。
两模式全档位内存代号 / 文件代号 / md5 全部回到目标代；`pb5.py`（三代改同一 2 MiB 块内不同 4 KiB 页）ALL PASS。
`pb2.py` 同机：只读 192 MB 导出 11.6 MB、只改 12 MB 导出 20.4 MB，与写跟踪位图逐档相等。

读法：修复后 memfile = 该档写入量 + 几 MB 系统本底，随写入量线性，从此与本方案的内存差分**可相减**；
`touch1` 修复前后在噪声内（第一版实现曾 +100 ~ 200 ms，零拷贝快路径 + 缓冲池后回落）。
同一二进制上本方案自己的 `checkpoint_verify.py` 全过、`checkpoint_bench_v2.py` 六代全 `incremental`
（§4）。950 上尚未复跑；两台机器代码路径相同，差别只在位图供数机制（写保护 vs HDBSS），
口径见[第 27 篇 §5.5](27-cross-implementation.md#55-原生快照口径修复后)，机理见[第 21 篇](21-native-increment-diagnosis.md)。

#### 表 3-K · `pb2.py` 三列并排（修复前后）

**条件**：`rollback/scripts/probes/pb2.py`，模板 `base` 2 GiB、`huge_pages=2M`；每档真做一次 `create_snapshot`，
档与档之间沙箱经历 pause + resume。左列是修复前原生 pause 的判据 `GET /memory/dirty`（2 MiB 粒度），
中列是 Firecracker 写跟踪位图 `PUT /snapshot/save-dirty-bitmap`（4 KiB 粒度），右列是实际导出的 memfile。

| 步骤 | 950 HDBSS（09-10，修复前）<br>`/memory/dirty` / 位图 / 导出 | 920B kvm-wp（09-10，修复前）<br>`/memory/dirty` / 位图 / 导出 | 920B kvm-wp（09-11，修复后）<br>位图 / 导出 |
|---|---|---|---|
| A 刚建好 | 118.0 / 4.6 / 118.0 | 154 / 4.1 / 154 | 5.5 / 5.5 |
| B 写 192 MiB | 324.0 / 205.2 / 324.0 | 370 / 219.8 / 370 | 209.5 / 209.5 |
| C 空转 | 122.0 / 3.9 / 122.0 | 162 / 4.1 / 162 | 4.5 / 4.5 |
| D 只读 192 MiB | 324.0 / 15.0 / 324.0 | 358 / 14.6 / 358 | **12.7 / 12.7** |
| E 空转 | 134.0 / 3.8 / 134.0 | 152 / 4.1 / 152 | 4.1 / 4.1 |
| F 只改 12 MiB | 148.0 / 19.7 / 148.0 | 176 / 20.5 / 176 | **20.3 / 20.3** |

`pb4.py`（920B，修复前）把 4 KiB 位图归并到 2 MiB 差分块，得到「只换判据、不改粒度」的预测导出量：
空转 134、只读 192 MiB 148、只改 12 MiB 156、只改 48 MiB 192、写 192 MiB 358 MiB ——
相对修复前分别降 17% / 60% / 15% / 13% / 6%，每代约 130 MiB 是归并地板。

读法：修复前左列与右列逐行相等（原生 pause 按它导出），D 行「只读」涨 192 MiB 说明读被算成脏；
中列在两台机器、两种后端下形状一致，都是「写入量 + 几 MiB 本底」，说明位图本身精确。修复后右列 = 中列。
机理与三层拆解见[第 21 篇](21-native-increment-diagnosis.md)。

---

## 4. 性能 · 920B（软件写保护）

**所有数字都含 VM exit 开销。** 920B 没有 HDBSS，每个干净页的第一次写都要陷出一次虚机，
这笔账全部落在下面这些数字里，**换到有硬件标脏的机器上会消失**
（[第 7 篇 §6.1](07-dirty-page-tracking.md#61-软件写保护的代价模型)）。

### 4.1 全量 vs 增量（920B-0824-bench）

**条件**：920B · `kvm-wp` · 模板 2 vCPU / 2 GB · ext4 方案落 loop ext4（`/mnt/ext4dev`）、
XFS 方案落真盘 XFS（`/`）· 每档一个全新沙箱，链深一致从 1 长到 33，预热 3 + 计入 30。

| 脏页档 | ext4 create p50 | p90 | p99 | max | 相对全量 | 新增物理空间 | XFS create p50 | XFS 新增物理空间 |
|---|---|---|---|---|---|---|---|---|
| **全量（2 GB）** | **1.511 s** | 2.191 s | 2.215 s | 2.215 s | 1.0× | 2048.1 MB | **1.524 s** | 2048.1 MB |
| 0 MB | **29.5 ms** | 31.3 | 34.9 | 34.9 | 快 51× | 4.58 MB | **109.6 ms** | 36.03 MB |
| 16 MB | **42.3 ms** | 46.4 | 48.0 | 48.0 | 快 36× | 23.24 MB | **132.3 ms** | 57.17 MB |
| 32 MB | **48.8 ms** | 52.1 | 57.1 | 57.1 | 快 31× | 39.81 MB | **143.4 ms** | 74.03 MB |
| 64 MB | **70.2 ms** | 74.7 | 77.5 | 77.5 | 快 22× | 73.04 MB | **162.0 ms** | 107.09 MB |
| 128 MB | **111.1 ms** | 116.2 | 118.8 | 118.8 | 快 14× | 139.26 MB | **186.9 ms** | 173.71 MB |
| 256 MB | **196.2 ms** | 204.2 | 218.5 | 218.5 | 快 8× | 271.90 MB | **276.8 ms** | 307.07 MB |

> **数值以 `bench.json` 为准**。同一轮的 `报告.md` 与
> [`../scripts/dev/耗时基准结论.md`](../scripts/dev/耗时基准结论.md) 对同一个值取了不同的小数位
> （0 MB 档 create p50 在两处分别写作 0.029 s 与 0.030 s，浅集 p50 写作 0.0315 s 与 0.032 s）。
> 原始样本的 p50 是 **0.0295 s** 与 **0.0315 s**，本篇一律按原始值换算。

三条读法：**两套的全量几乎相同**（1.511 vs 1.524 s，都是把 2 GB 整份写出去，跟方案无关，
互为佐证）；**增量把全量甩开一到两个数量级，且只花掉脏页那么多空间**（ext4 那一列
4.58 → 271.90 MB 对应 0 → 256 MB 档）；**XFS 方案每次固定多花约 33 MB**，各档一致 ——
那是 `cowextsize` 默认 128 KB 在写零散 4 KB 脏页时的分配粒度放大，
把仓库目录设成 `cowextsize 4096` 之后**整个消失**（4.92 / 40.6 / 272.21 MB），
代价是创建耗时涨约 6%（[第 18 篇 §6.2](18-ext4-vs-xfs.md#62-决策表)）。

### 4.2 冻结窗口（920B-0824-bench）

| 脏页档 | ext4 冻结 p50 | max | 占墙钟 | 底噪 | XFS 冻结 p50 | 占墙钟 | 底噪 |
|---|---|---|---|---|---|---|---|
| 0 MB | 8.6 ms | 59.0 | 64% | 0.8 ms | 15.3 ms | 26% | 0.9 ms |
| 16 MB | 43.3 ms | 76.4 | 89% | 3.5 ms | 81.6 ms | 48% | 12.6 ms |
| 32 MB | 48.2 ms | 89.9 | 91% | 3.1 ms | 83.0 ms | 51% | 5.2 ms |
| 64 MB | 89.0 ms | 120.7 | 94% | 5.6 ms | 104.2 ms | 58% | 3.5 ms |
| 128 MB | 106.4 ms | 129.3 | 96% | 10.8 ms | 143.8 ms | 67% | 4.4 ms |
| 256 MB | 188.4 ms | 210.0 | 97% | 3.3 ms | 241.9 ms | 78% | 4.2 ms |

十二格**全部标「可区分 ✓」**（判据 `min(冻结) > 2 × 底噪`）。
**ext4 方案的墙钟几乎全是冻结**（89–97%）—— 这条路上没有「虚机还在跑」的部分可省；
**XFS 方案只有一半左右是冻结**（reflink 克隆在虚机还跑着的时候做），
按「业务感受到的停顿」这个口径两套差距比主表小得多（32 MB 档 83 vs 48 ms，而不是 143 vs 49 ms）。
**但冻结是解释项，不是判据**（[第 26 篇 §1.2](26-performance-methodology.md#12-口径定义表)）。

> **0 MB 档的冻结表里「客户端墙钟 p50」是 13.4 ms，而档位表里是 29.5 ms。**
> 两者**不是同一批样本**：冻结那一段跑的是另一个沙箱、另一条更浅的链
> （一次 seed + 8 次，链深约 9），档位表是链深 1→33 的 30 个样本。差的正是链深
> （[第 26 篇 §4.3](26-performance-methodology.md#43-c冻结窗口怎么测)）。
> **§5 的判定表一律取档位表的值**，因为那才是配平过链深、样本量足够的一批。

### 4.3 restore（920B-0824-bench）

| 脏页档 | ext4 浅集 p50 | 浅集 p99 | 深集 p50（33 步） | XFS 浅集 p50 | 浅集 p99 | XFS 深集 p50 |
|---|---|---|---|---|---|---|
| 0 MB | 31.5 ms | 43.2 | 43.9 ms | 20.3 ms | 63.9 | 24.7 ms |
| 16 MB | 34.2 ms | 55.5 | 45.5 ms | 20.3 ms | 215.4 | 25.3 ms |
| 32 MB | 43.9 ms | 60.0 | 53.9 ms | 24.2 ms | 296.0 | 28.7 ms |
| 64 MB | 43.2 ms | 54.3 | 54.4 ms | 24.3 ms | 229.5 | 31.5 ms |
| 128 MB | 57.5 ms | 85.9 | 68.1 ms | 41.3 ms | 347.9 | 97.8 ms |
| 256 MB | 77.8 ms | 118.7 | 116.6 ms | 35.8 ms | 295.9 | 90.2 ms |

**两套所有 restore 的内容校验全部一致，`restore_mismatches` 为 0**——
落错代的耗时数字是没有意义的，这一条先过关才能读上面的时间
（[第 26 篇 §3.4](26-performance-methodology.md#34-退一代的代价归上一行)）。

XFS 方案的浅集 p50 更快（每代是一份逻辑完整的内存视图，恢复时不用沿差分树凑回滚集），
**但 p99 明显毛糙**（215–348 ms，是 p50 的 10 倍上下），ext4 方案的 p99 稳定在 p50 的
1.5 倍以内。这条长尾**未定位**，记在 §8。

### 4.4 60 秒持续连打（920B-0824-bench）

| | ext4 方案 | XFS 方案 |
|---|---|---|
| 完成次数 | 450 | 236 |
| 速率 | **7.49 次/秒** | **3.93 次/秒** |
| p50 / p99 / max | 89.6 / 199.7 / 214.1 ms | 218.1 / 325.3 / 345.2 ms |
| 前 1/3 → 后 1/3 p50 | 54.9 → 154.4 ms（**×2.81**） | 162.3 → 278.8 ms（**×1.72**） |
| 每次物理占用 | 39.79 MB | 75.98 MB |

两套都是**平滑单调的爬升**，没有台阶、没有毛刺。含义与根因在 §7。

### 4.5 漂移自查（920B-0824-bench）

判据表，不是数据表：每档前 1/3 的 p50 对后 1/3 的 p50，两列接近才说明这一档内部没有在漂、
p50 能代表整档。

| 脏页档 | ext4 前 1/3 → 后 1/3 | XFS 前 1/3 → 后 1/3 | `mem_mode` |
|---|---|---|---|
| 0 MB | 28.7 → 29.7 ms | 93.1 → 121.6 ms | `incremental` |
| 16 MB | 40.7 → 44.4 ms | 111.6 → 145.7 ms | `incremental` |
| 32 MB | 47.6 → 49.1 ms | 121.0 → 154.7 ms | `incremental` |
| 64 MB | 68.8 → 72.0 ms | 148.7 → 168.1 ms | `incremental` |
| 128 MB | 110.5 → 111.0 ms | 169.1 → 214.2 ms | `incremental` |
| 256 MB | 193.8 → 199.5 ms | 254.7 → 307.2 ms | `incremental` |

**十二档 `mem_mode` 全部是 `incremental`**，两轮都成立 —— 判定的前提条件满足。
ext4 方案六档两列都很近；**XFS 方案六档两列都偏离 20–30%**，说明那一轮 XFS 各档内部
本身在漂，它的 p50 代表性弱于 ext4 那一列。读 §5 的 XFS 判定时要带上这一条。

### 4.6 08-25：两套在同条件下的最终对照

08-24 那两轮 **ext4 跑在 loop 上、XFS 跑在真盘上**，不可直接相比。
08-25 把两套都放到同参数的 300 G loop 上（`direct-io` / 预分配 / 4K 扇区 / `nodiscard`），
XFS 那边补上 `cowextsize=4096`，这才是同条件对照。

| 脏页档 | ext4 create p50 | XFS create p50 | ext4 冻结 p50 | XFS 冻结 p50 | ext4 浅集 p50 | XFS 浅集 p50 | ext4 深集 | XFS 深集 | ext4 空间 | XFS 空间 |
|---|---|---|---|---|---|---|---|---|---|---|
| 全量 | 1.549 s | 1.505 s | —— | —— | —— | —— | —— | —— | 2048.1 MB | 2048.2 MB |
| 0 MB | **22 ms** | 133 ms | **7.3 ms** | 14.9 ms | 29.0 ms | **19.5 ms** | 36.8 ms | **27.0 ms** | 4.16 MB | 4.92 MB |
| 16 MB | **33 ms** | 141 ms | **35.3 ms** | 101.3 ms | 32.8 ms | **19.5 ms** | 48.3 ms | **27.6 ms** | 23.35 MB | 23.54 MB |
| 32 MB | **42 ms** | 171 ms | **44.9 ms** | 97.4 ms | 38.4 ms | **20.7 ms** | 61.5 ms | **27.4 ms** | 39.74 MB | 40.6 MB |
| 64 MB | **62 ms** | 189 ms | **65.5 ms** | 120.7 ms | 42.2 ms | **23.3 ms** | 57.5 ms | **32.7 ms** | 73.04 MB | 73.51 MB |
| 128 MB | **104 ms** | 223 ms | **123.1 ms** | 161.9 ms | 58.2 ms | **26.1 ms** | 70.4 ms | **32.5 ms** | 139.24 MB | 139.8 MB |
| 256 MB | **183 ms** | 306 ms | **183.2 ms** | 254.3 ms | 88.6 ms | **35.7 ms** | 125.7 ms | **96.7 ms** | 271.93 MB | 272.21 MB |

**这是一个明确的取舍，不存在全面更优的一方**：ext4 方案 create 快 1.7–6×、冻结短 1.4–2.2×；
XFS 方案 restore 快 1.3–2.5×，但 **0 / 16 MB 两档的 restore p99 高达 467 / 454 ms**
（是 p50 的 24 倍，**未定位**，记在 §8）；盘上占用在设了 `cowextsize` 之后打平。
本方案的热路径是 create 与冻结窗口，选型判据的完整讨论在
[第 18 篇 §6](18-ext4-vs-xfs.md#6-怎么选)。

### 4.7 链深对照（920B-0904）

**条件**：920B · `kvm-wp` · ext4 方案 · 产物落**真盘 XFS** · 每代写脏不同的 4 MB（错开 `seek`，
内容解析的最坏情况）· `timing.py` 末行 `ALL CORRECT`。

| 链深 | create p50 | 深集回退 | 深集前滚 | 浅集 p50 | `df` 增量 |
|---|---|---|---|---|---|
| 5 | 64 ms | 49 ms | 45 ms | 37 ms | 2113 MB |
| 20 | 62 ms | 76 ms | 72 ms | 39 ms | 2388 MB |
| 50 | 63 ms | 124 ms | 112 ms | 46 ms | 2922 MB |

**create 不随链深增长**（64 / 62 / 63 ms）。**深集随链深涨、浅集几乎不涨**（37 / 39 / 46 ms）——
这正是「回滚集大小」与「链深」被解开的那一刀：深集的路径纪元数跟着链深走，
浅集恒为 1 个纪元，只有目标所处的深度在变。链深本身只影响位测试次数，
真正的 I/O 只发生在命中的那一次（[第 8 篇 §7.1](08-memory-diff-tree.md#71-恢复为什么不随链深变慢)）。
**所以本方案不需要周期性压平历史。**

同一轮的脏页量扫描（A 段）顺带印证了成本模型是 O(本代脏页)：

| 代 | 脏页 | create | `df` 增量 | `mem_mode` |
|---|---|---|---|---|
| g1 | 0 MB | 1.208 s | 2049 MB | `full` |
| g2 | 64 MB | 105 ms | 82 MB | `incremental` |
| g3 | 256 MB | 201 ms | 273 MB | `incremental` |
| g4 | 512 MB | 353 ms | 536 MB | `incremental` |
| g5 | 0 MB | 65 ms | 20 MB | `incremental` |

### 4.8 主力脚本与对照组脚本互印（920B-0908）

**条件**：920B · `kvm-wp` · 模板 `base`（2 vCPU / 2048 MB）· 产物文件系统 XFS ·
`orchestrator-ext4-ecdad325c-pausefix` · 2026-09-08。
对照组脚本默认不做落盘静默，所以对齐的是主力脚本「写完立刻拍」那一段。

| 名义 内存/文件 | 主力① e2e | 对照组 e2e | 主力① 内存产物 | 对照组 内存产物 | 主力① 文件产物 | 对照组 文件产物 |
|---|---|---|---|---|---|---|
| 全量 | 1.435 s | 1.627 s | —— | 2048 MB | —— | —— |
| 0 / 0 | 56 ms | 68 ms | 8.3 MB | 16.9 MB | 0.0 MB | 0.4 MB |
| 12 / 4 | 84 ms | 85 ms | 27.7 MB | 30.7 MB | 4.4 MB | 4.7 MB |
| 24 / 8 | 115 ms | 87 ms | 43.3 MB | 46.9 MB | 8.5 MB | 8.6 MB |
| 48 / 16 | 108 ms | 124 ms | 75.7 MB | 80.7 MB | 16.4 MB | 16.7 MB |
| 96 / 32 | 180 ms | 193 ms | 143.6 MB | 146.0 MB | 32.4 MB | 32.8 MB |
| 192 / 64 | 276 ms | 315 ms | 275.6 MB | 276.5 MB | 64.5 MB | 64.8 MB |

**产物量差 0.3%**（最大档 275.6 vs 276.5 MB），两个脚本造负载的方式等效；耗时上对照组
系统性高 5–15%（24/8 那档反了，是单次抖动 —— 两个脚本每档都只测一次）。
读表的固定预期也成立：**实测内存 ≈ 名义内存 + 名义文件 + 本底**（192 + 64 + 本底 ≈ 276）、
**实测文件 ≈ 名义文件**（[第 26 篇 §2.5](26-performance-methodology.md#25-文件那半故意不用-o_direct)）。

同一轮的时机成本（主力脚本独有，① 立刻拍 减 ② 冲干净再拍）：

| 名义 | ① 立刻拍 | ② 冲干净再拍 | ② / ① |
|---|---|---|---|
| 0 / 0 | 56 ms | 61 ms | 1.10× |
| 12 / 4 | 84 ms | 72 ms | 0.86× |
| 24 / 8 | 115 ms | 97 ms | 0.85× |
| 48 / 16 | 108 ms | 99 ms | 0.92× |
| 96 / 32 | 180 ms | 164 ms | 0.92× |
| 192 / 64 | 276 ms | 228 ms | 0.83× |

最大档差 48 ms。**这是「写完立刻拍」这个时机的成本，不是快照机制的成本**：
刚写的文件数据正被内核往盘上刷、和写 `mem_diff` 抢磁盘，加上 NBD 管道里的在途数据要排空。
客户如果在写完的同一毫秒就 checkpoint，拿到的是 ① 那一列。

> **这一轮三个脚本全程 `mem_mode=?`**（SDK 不带该字段），
> 防「增量静默退化成全量」的判据是空的，靠 §2.2 那条旁证顶。
> 按[第 26 篇 §8](26-performance-methodology.md#8-怎么读一份报告) 的规矩，
> 这一轮的数据只能用来做**脚本之间的互印**，**不进 §5 的判定表**。

---

## 5. 对照客户指标的判定表

判定口径**全部**按[第 26 篇 §1.2](26-performance-methodology.md#12-口径定义表)：
客户端墙钟、增量 checkpoint 与原地 restore、**p50 判定 p99 并列**、按脏页量分档、
全量单列不判定、冻结窗口只作解释项。达标线照抄客户：**checkpoint ≤ 200 ms、restore ≤ 100 ms**。

### 5.1 920B · ext4 方案（判定主轮：920B-0824-bench）

**条件标签**：920B（Kunpeng 920）· 脏页后端 `kvm-wp`（**每个干净页的第一次写都要陷出一次虚机，
这笔开销全含在下面每一格里**）· 产物在调优过的 loop ext4 上 · 模板 2 vCPU / 2 GB ·
2026-08-24 · `mem_mode` 六档全 `incremental` · `restore_mismatches` = 0。

| 脏页档 | checkpoint p50 | 达标 | checkpoint p99 | 达标 | restore 浅集 p50 | 达标 | 浅集 p99 | 达标 | 深集 p50（n=4） | 达标 | 冻结 p50（解释项） |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 0 MB | 29.5 ms | ✓ | 34.9 ms | ✓ | 31.5 ms | ✓ | 43.2 ms | ✓ | 43.9 ms | ✓ | 8.6 ms |
| 16 MB | 42.3 ms | ✓ | 48.0 ms | ✓ | 34.2 ms | ✓ | 55.5 ms | ✓ | 45.5 ms | ✓ | 43.3 ms |
| 32 MB | 48.8 ms | ✓ | 57.1 ms | ✓ | 43.9 ms | ✓ | 60.0 ms | ✓ | 53.9 ms | ✓ | 48.2 ms |
| 64 MB | 70.2 ms | ✓ | 77.5 ms | ✓ | 43.2 ms | ✓ | 54.3 ms | ✓ | 54.4 ms | ✓ | 89.0 ms |
| 128 MB | 111.1 ms | ✓ | 118.8 ms | ✓ | 57.5 ms | ✓ | 85.9 ms | ✓ | 68.1 ms | ✓ | 106.4 ms |
| 256 MB | 196.2 ms | ✓ | **218.5 ms** | **✗** | 77.8 ms | ✓ | **118.7 ms** | **✗** | **116.6 ms** | **✗** | 188.4 ms |
| **全量（2 GB）** | 1.511 s | **一次性成本，不参与判定** | 2.215 s | —— | —— | —— | —— | —— | —— | —— | —— |

**判定结论（按 p50）：六档全部达标。**
**并列的 p99 在最高档越线三处**：checkpoint p99 218.5 ms（超 18.5 ms）、
restore 浅集 p99 118.7 ms、深集 p50 116.6 ms。

三条不能省略的限定：

1. **256 MB 那一档是本方案的临界点**，而且是在**没有硬件标脏**的机器上测的。
   p50 196.2 ms 距离 200 ms 只剩 3.8 ms —— 这一档的达标是「压线过」，不是「有余量」。
2. **深集那一列样本量只有 4**（`bench-ckpt.py` 的深集是 tip ↔ root 往返 2 轮 = 4 次跳，
   [第 26 篇 §4.4](26-performance-methodology.md#44-restore浅集与深集)）。
   n=4 的 p50 抗抖能力很弱，256 MB 那一格的 ✗ **应当理解为「已经越线，且需要更多样本才能定量」**，
   不能理解为「越线 16.6 ms」这么精确的一句话。
3. 同一台机器的 08-25 那一轮（两套都在 300 G loop 上）复核了同一个形状：
   checkpoint p50 22 → 183 ms 全达标、p99 在 256 MB 档 216 ms 越线；
   restore 浅集 p50 29 → 88.6 ms 全达标、256 MB 档 p99 122.5 ms 与深集 125.7 ms 越线。
   **两轮的结论一致：p50 全过，最高档的 p99 与深集不过。**

### 5.2 920B · XFS 方案（对照，不是交付判定对象）

同轮同口径。XFS 方案落的是**真盘 XFS**，与 ext4 方案那一列**不同条件，不可相减**。

| 脏页档 | checkpoint p50 | 达标 | checkpoint p99 | 达标 | 浅集 p50 | 达标 | 浅集 p99 | 达标 | 深集 p50（n=4） | 达标 |
|---|---|---|---|---|---|---|---|---|---|---|
| 0 MB | 109.6 ms | ✓ | 188.3 ms | ✓ | 20.3 ms | ✓ | 63.9 ms | ✓ | 24.7 ms | ✓ |
| 16 MB | 132.3 ms | ✓ | 192.5 ms | ✓ | 20.3 ms | ✓ | **215.4 ms** | **✗** | 25.3 ms | ✓ |
| 32 MB | 143.4 ms | ✓ | 184.7 ms | ✓ | 24.2 ms | ✓ | **296.0 ms** | **✗** | 28.7 ms | ✓ |
| 64 MB | 162.0 ms | ✓ | **259.2 ms** | **✗** | 24.3 ms | ✓ | **229.5 ms** | **✗** | 31.5 ms | ✓ |
| 128 MB | 186.9 ms | ✓ | **255.7 ms** | **✗** | 41.3 ms | ✓ | **347.9 ms** | **✗** | 97.8 ms | ✓ |
| 256 MB | **276.8 ms** | **✗** | **349.8 ms** | **✗** | 35.8 ms | ✓ | **295.9 ms** | **✗** | 90.2 ms | ✓ |

**XFS 方案在 restore 那一半优于 ext4 方案（深集全档达标），在 checkpoint 那一半差得多
（最高档 p50 就越线，p99 从 64 MB 档起全线越线）。** 再叠上 §4.5 那条：
这一轮 XFS 六档的漂移自查两列都偏离 20–30%，**它的 p50 代表性弱于 ext4 那一列**。

### 5.3 950 · ext4 方案【待测】

**条件标签**（跑之前就能填的部分）：950（Kunpeng 950 `slot6`）· 脏页后端 `hdbss` ·
产物在**真盘 ext4** 上 · 模板待定 · 二进制待记录。

| 脏页档 | checkpoint p50 | 达标 | checkpoint p99 | 达标 | 浅集 p50 | 达标 | 浅集 p99 | 达标 | 深集 p50 | 达标 | 冻结 p50 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 0 MB | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |
| 16 MB | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |
| 32 MB | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |
| 64 MB | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |
| 128 MB | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |
| 256 MB | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 | 待测 |
| **全量** | 待测 | **不参与判定** | 待测 | —— | —— | —— | —— | —— | —— | —— | —— |

整张表由 `bench-ckpt.py` 一次跑出（末行必须是 `BENCH OK`，六档 `mem_mode` 必须全
`incremental`）。**预期方向而非承诺**：HDBSS 把软件写保护的 VM exit 去掉，
受益最大的应当是冻结窗口那一列，因为那笔开销正好发生在暂停窗口里
（[第 7 篇 §6.1](07-dirty-page-tracking.md#61-软件写保护的代价模型)）。
**在 950 上真跑出来之前，这句话是推论，不是结果。**

### 5.4 那个 100 ms 为什么不能拿来判定

950 唯一有数的一轮里，`checkpoint.restore` 的 p50 是 **0.100 s**，
恰好落在客户指标那条线上。这个巧合很容易被当成「950 上 restore 刚好达标」，
但它不构成判定，四条理由都是硬的：

1. **样本量与构造**：10 次，且这 10 次混了退一代（2 次）、跨 2 代前滚（1 次）、
   A/C 交替（6 次）、杀进程后回滚（1 次）四种跳法。判定要求**浅集与深集分列**，
   这一批混在一起，任何一个 p50 都不属于其中任何一列。
2. **不在档位上**：每代脏内存 128 MB + 根文件系统 32 MB，名义总量约 160 MB，
   既不是 128 MB 档也不是 256 MB 档；而且现场是**重建**的，分配成本混在里面。
3. **链深不可比**：那条链只有 3 代，判定表的链深是 33。
4. **最大值 153 ms**：10 个样本里的 max 已经超过 100 ms，没有 p99 可言。

结论一句话：**它是一个「量级正确」的旁证，不是一个判定值。**
950 的判定要等 §5.3 那张表填满。

### 5.5 全量为什么单列

树根必须自包含，否则一条链没有可解析的底
（[第 8 篇 §6.3](08-memory-diff-tree.md#63-全量根为什么是默认)）。它的代价是写整份 guest RAM，
920B 上 2 GB 模板实测 p50 1.511 s，比任何增量档都高一个数量级。

但它**每个沙箱只发生一次**：一个沙箱做 100 次 checkpoint，这 1.5 s 摊下来是 15 ms/次，
而且不在「回退到上一步」这个动作的路径上。把它算进判定，得到的既不反映客户体验、
也不反映实现质量。

**它单列的另一个作用是当分母**：没有 1.511 s 这一行，「增量快 51 倍」这句话没法说。

---

## 6. 附表：与 gsd/CRIU 及原生 snapshot 的实测并列

**只并列，不作差。** 三套的快照范围不同 —— 进程级只装选中进程的地址空间，
虚机级装整个 guest RAM —— 所以有些列语义就不一致
（[第 27 篇 §4.5](27-cross-implementation.md#45-哪些列可以并排)）。
措辞统一为「**快照范围不同，所以快的地方不同**」，不写优劣
（[第 27 篇 §6](27-cross-implementation.md#6-定性结论)）。

### 6.1 与进程级快照（gsd / CRIU）· 920B-0908

**条件标签（两边同轮同机）**：920B · 脏页后端 **`kvm-wp`** · 模板 `base` ·
本方案产物落宿主机 `/orchestrator/build/checkpoints`（XFS 真盘）、
进程级产物落沙箱内 `/var/lib/gsd/snapshots/` · 2026-09-08 15:18–15:21 ·
本方案用对照组脚本、进程级用它自己的对照脚本，**档位表逐格相同、负载造法相同**。

checkpoint 端到端与产物量：

| 档 名义 内存/文件 | 本方案 e2e | 进程级 e2e | 本方案 内存产物 | 进程级 CRIU 内存产物 |
|---|---|---|---|---|
| cp0 全量 | 1627.4 ms | 687.4 ms | 2048 MB | 196 MB |
| cp1 0 / 0 | 68.5 ms | 143.9 ms | 16.9 MB | 193 MB |
| cp2 12 / 4 | 85.1 ms | 30.8 ms | 30.7 MB | 14 MB |
| cp3 24 / 8 | 87.1 ms | 38.6 ms | 46.9 MB | 26 MB |
| cp4 48 / 16 | 123.7 ms | 54.9 ms | 80.7 MB | 50 MB |
| cp5 96 / 32 | 192.6 ms | 81.8 ms | 146.0 MB | 98 MB |
| cp6 192 / 64 | 314.7 ms | 127.9 ms | 276.5 MB | 194 MB |

restore 端到端：

| 跳（目标代） | 本方案 e2e | 进程级 e2e |
|---|---|---|
| → cp1 | 126.4 ms | 196.7 ms |
| → cp2 | 70.1 ms | 191.9 ms |
| → cp3 | 64.6 ms | 186.2 ms |
| → cp4 | 75.9 ms | 231.0 ms |
| → cp5 | 78.7 ms | 149.7 ms |
| → cp6 | 145.0 ms | 158.1 ms |

**证据在产物量里**：进程级 cp6 的 CRIU 产物 194 MB ≈ 那个常驻进程 192 MB 堆的脏量，
文件那 64 MB **完全不在内存产物里**（由 overlayfs 单独管）；本方案同档 276.5 MB ≈
192（内存）+ 64（那次文件写留在 guest 页缓存里的页）+ 本底。两个数字都能从各自的覆盖范围
推出来 —— 定性结论与它的两条语义推论在
[第 27 篇 §6](27-cross-implementation.md#6-定性结论)，这里只放数字。

读这两张表必须带上的四条限定：

- **本方案 cp1 那一行反常**（68.5 ms 的 e2e，产物只有 16.9 MB 却比 cp2 的 12 MB 档还慢），
  原因见 §7.2。**进程级 cp1 同样反常**（143.9 ms，比 cp2 的 30.8 ms 慢 4.7 倍），
  两边现象类似，**进程级那一侧的机理未查**。
- **进程级 cp1 的产物 193 MB 与 cp2 的 14 MB 之间那个跳变没有解释。**
  按覆盖范围推，cp1（名义 0/0）本该最小。原始材料只记录了数值，没有给出原因，
  本篇**如实照抄，不做解读**；单次数据，不宜过度推断。
- **进程级那一侧的文件负载走 SDK 上传**（HTTP 推几十兆），本方案是 guest 内 `dd` 生成。
  这一段是否计进它的 checkpoint 计时，**源材料里两句话方向相反**：
  一句写「E2E 里 gsd 那边含上传时间」，紧接着一句写「从 `overlay_sink_ms` 只有 0.3~0.8 ms 看，
  这部分没有计进 checkpoint 计时」。**未裁决**，两个说法都记在这里；
  它只影响进程级那一列的 checkpoint e2e，不影响 restore 列与产物列。
- **两轮跨实现对照都跑在 `kvm-wp` 上。** 这个条件对三套的影响**不对称**：
  软件写保护的 VM exit 全部落在虚机级那两套身上，进程级走 soft-dirty，基本不受影响
  （[第 27 篇 §5.3](27-cross-implementation.md#53-三套的脏页判据)）。
  **换到 HDBSS 上这张表很可能变样，届时要整轮重跑，不是补一列。**

### 6.2 与 e2b 原生 snapshot · 950-native

数据在 §3.3 的表 3-I。三条并列时的口径：

| 列 | 能不能逐行并排 | 理由 |
|---|---|---|
| checkpoint 端到端 | 可并排 | 档位表逐格相同 |
| 内存产物量 | **可并排，不可相减**（950-native 轮）；修复后**可相减**（表 3-J） | 950-native 轮原生的判据把读也算脏，量的是工作集；2026-09-11 起原生改读与本方案同一份写跟踪位图 |
| restore / resume 端到端 | **只可并列** | 恢复语义不同：本方案是原地回滚（沙箱 ID 与 Firecracker 进程都不变），原生 snapshot 是**另起一个新沙箱** |
| `touch`（恢复后首次整读） | **仅原生非零，且必须加进它那一边** | 原生是懒加载，950 上这笔约 165 ms，比 resume 本身（约 90 ms）还大 |
| `upload` | 仅原生有 | 本方案产物就地可用 |
| 脏页后端 / `mem_mode` | 仅本方案有 | 另两套没有对应判据 |

还有一条介质差异必须说：本方案的产物在 `/orchestrator/build/checkpoints`，**真落盘**；
原生快照的最终产物默认落 `/tmp/templates`，而这两台机器的 `/tmp` 恰好是 **tmpfs**——
也就是在内存里。这不是 e2b 的设计，是 `LOCAL_TEMPLATE_STORAGE_BASE_PATH` 没设时的默认值
碰上这两台机器的挂载布局。做对比时两套的产物写在不同介质上，原生那一列的 `upload`
换成磁盘存储会显著变大。

---

## 7. 已知劣化与含义

### 7.1 持续连打：60 秒里慢 2.8 倍

**条件**：920B-0824-bench · ext4 方案 · `kvm-wp` · loop ext4 · 每次弄脏 32 MB。

60 秒不停 checkpoint，前 1/3 的 p50 是 54.9 ms、后 1/3 是 154.4 ms，**×2.81**；
同一台机器 08-25 那一轮是 49 → 156 ms，**×3.22**。XFS 方案同一段是 ×1.72（08-24）／×1.67（08-25）。
两套都是**平滑单调的爬升**，没有台阶。脚本在 `degradation > 1.5` 时会自动在报告里写上
「单次的数字不能直接外推到高频场景」。

**爬升跟着代数走，不跟着写入量走**，这一条是判定出来的，不是看图看出来的
（`probe-ramp.py`，判据写在脚本里，见[第 26 篇 §6](26-performance-methodology.md#6-probe-ramppy一个判别式设计)）：

| 脏页档 | 次数 | 首 10% p50 | 末 10% p50 | 倍数 | 累计写入 | 每代摊 | 每 GB 摊 |
|---|---|---|---|---|---|---|---|
| 0 MB | 400 | 12.4 ms | 111.4 ms | ×9.0 | 2.20 GB | **0.261 ms** | 508.7 ms |
| 32 MB | 400 | 44.5 ms | 152.0 ms | ×3.4 | 17.46 GB | **0.284 ms** | 7.3 ms |
| 256 MB | 150 | 188.2 ms | 211.3 ms | ×1.1 | 41.50 GB | **0.163 ms** | 0.6 ms |

按**每代**归一化，三档之间只差 **1.74 倍**；按**每 GB**归一化，差 **847.8 倍**。
脚本判 `depth`。

> **0 MB 那一档的「累计写入」有两个值，两个都对，分母不同。** `ramp.json` 里的
> `total_gb` 是 **2.20 GB**，那是**含垫底那次 2 GB 全量**的累计；扣掉全量之后，
> 400 代增量自己只写了约 **0.19 GB**。本表照抄原始字段（2.20），
> 而「写了 0.19 GB 照样爬 9 倍」这句话用的是扣除后的值。

**根因定位到了一处**：杀沙箱之前把深链上每个文件逐个量下来，
`manifest.json` 从 **1 027 B 长到 85 268 B**（400 代，约 **213 字节/代**），
其余文件（`mem_bitmap` 65 560 B、`snapfile` 11 646 B、`rootfs.header` 16 104 → 16 184 B）
基本不变。对应到实现，`Entry.Rootfs.Layers` 是一个 `[]RootfsLayer{BuildID, Path}` 列表，
**每个 checkpoint 都把截止到自己的全部层完整记一遍**——单次 checkpoint 里有一块
**O(链深)** 的记账，每次重做。

爬升还**略微超线性**（0 MB 档每 40 代的增量从 2.4 ms 涨到 18.3 ms，不是恒定值），
说明不止这一处。**没有继续深挖**，记在 §8。

### 7.2 全量之后紧接的第一次增量偏慢

§4.8 的表里，cp1（名义 0/0）的 e2e 是 68.5 ms，比 cp2（12/4）的 85.1 ms 只低一点点，
而它的产物只有 16.9 MB。原因写在对照组脚本的注释里，是第一手材料：

> cp0 是 2 GB 全量，它的回写要刷好一阵；checkpoint 路径上有三次 fsync
> （`snapfile`、位图 sidecar、`mem_diff`），在 ext4 上 fsync 要等同一事务里别人的
> ordered 数据落盘，所以 cp1 会排在 cp0 后面。

脚本为此留了一个**诊断开关** `SETTLE_AFTER_FULL=1`：开了之后 cp0 与 cp1 之间先把宿主脏页
冲干净，开与不开各跑一次，cp1 的差值就是这笔排队。注释同时写明**不要默认打开**：

> 开了量的是静默宿主上的成本，而用户在全量之后紧接着做增量时真的要付那一下。
> 默认关着，cp1 那一行才是诚实的。

这是一条**测量纪律**多过一条缺陷：这笔排队是真实存在的用户成本，不能用测试开关把它藏掉。

### 7.3 对使用场景的含义与缓解方向

| 使用形态 | 单次数字能不能直接用 | 说明 |
|---|---|---|
| 短生命周期沙箱，几十次 checkpoint 就销毁 | **能** | §5 的判定表就是这个形态，链深 33 |
| 一个沙箱长期存活、持续高频 checkpoint | **不能** | 按实测约 0.28 ms/代估算，100 代约 +28 ms、400 代约 +110 ms |
| 刚建完树根就连着做增量 | 第一次要多付 | §7.2 那笔 fsync 排队 |

三条缓解方向，**都还没做**：把老 checkpoint `delete()` 掉；定期换沙箱；
或者改实现，让每代的 rootfs 视图**引用**父代而不是整份复制掉那张层列表
（这一条直指 `manifest.json` 那块 O(链深) 的记账，是最对症的一条，
列在[第 30 篇 §5.2](30-extending.md#52-可能的方向)）。

---

## 8. 尚未覆盖

这一张表把原先散在三处的缺口合并了（第 17 篇的实测状态、第 30 篇的两张缺口表、
开发态记录里的「还缺什么」）。**以后只维护这一份**，其余各篇引用过来。

| # | 缺口 | 要跑什么才能填上 | 影响 |
|---|---|---|---|
| 1 | **950 上的分档基准** | `bench-ckpt.py` 一轮（填 §5.3、表 3-B/C/D/E）、`checkpoint_bench.py` 一轮（填表 3-A） | 950 的达标判定整张空着 |
| 2 | **HDBSS 与软件写保护的对照** | 在同一台 950 上跑两遍 `bench-ckpt.py`，一遍 `hdbss`、一遍强制 `kvm-wp`，其余条件不动 | 硬件标脏的收益**尚未量化**；跨机器相减得不到它（§1.2） |
| 3 | **950 上的树语义 / pause / 兼容矩阵 / 稳定性** | `correctness.py`、`pause_verify.py`、`compat_matrix.py`、`loop.py` 各一轮 | 正确性结论目前靠从 920B 外推 |
| 4 | **950 上的链深对照** | `timing.py`（填表 3-F） | 「恢复不随链深变慢」在硬件标脏上未验 |
| 5 | **950 上的跨实现对照** | `checkpoint_bench_v2.py`（填表 3-H） | 950 上只有原生那一列有数（表 3-I） |
| 6 | **HDBSS 三级证据的 L3 落盘** | `hdbss_evidence.py`，950 上冷/热写耗时比 | L1/L2 已取到，L3 只在终端看过 |
| 7 | **`FC_HDBSS_ORDER` 取 1 / 2 / 4 的对比** | 三轮 `bench-ckpt.py`，只改这一个环境变量 | 写密集负载下 buffer 溢出会吃掉多少收益，不知道（[第 7 篇 §6.2](07-dirty-page-tracking.md#62-hdbss-的-buffer-与溢出)） |
| 8 | **连打爬升里超线性的那一段** | 无现成脚本，要在 `manifest.json` 之外继续定位 | 已定性到「跟代数走」并找到一处 O(链深) 记账，但**不止这一处**（§7.1） |
| 9 | **XFS 方案 restore 的 p99 长尾** | 无现成脚本；先复现 0 / 16 MB 两档 467 / 454 ms 的尾巴 | §5.2 里 restore p99 全线越线的直接原因，**未定位** |
| 10 | **XFS 方案的宿主分段与 `cowextsize=4K` 下的完整矩阵** | `bench-ckpt.py` 在 `cowextsize=4096` 下跑满六档 | 现有 XFS 耗时表测于默认 128 KB，空间表测于 4 KB，两者不同轮 |
| 11 | **`conntrack` 那 7.5 ms 能不能省** | 无现成脚本 | 它是 restore 冻结窗口里一笔完全不随脏页量变化的固定税，0 MB 档占三分之一还多 |
| 12 | **跨树回滚** | 无测试覆盖 | 断链后新根开新树，跨树回滚会退化成全量；逻辑上正确（[第 8 篇 §4.5](08-memory-diff-tree.md#45-最近公共祖先怎么求)）但没测 |
| 13 | **并发操作同一沙箱** | 无针对性的竞态测试 | 依赖 `opLocks`（[第 15 篇 §8](15-state-and-concurrency.md#8-单元测试守着哪些不变量)） |
| 14 | **HDBSS 武装时序** | 只靠调用点位置保证，没有断言 | 不变量 #13 无测试守护 |
| 15 | **完整 `rpmbuild` 流程** | 尚未端到端跑通 | 交付形态未验证 |
| 16 | **二进制 sha 与模板规格的留档** | 补进 `00-context.md`（[第 29 篇 §5](29-acceptance-runbook.md#5-结果回传规范)） | 08-24 / 08-25 / 08-29 三轮的这两项**空着**，事后补不上（§1.3） |

> 第 16 条是这张表里唯一一条**不需要机器就能修**的：它是流程缺口，不是能力缺口。
> 一轮基准跑完，条件标签没记全，那轮数据的可引用性就永久受损。

---

## 9. 小结

1. **950 与 920B 的数字只能并列，不能相减。** 脏页后端、产物介质、产物文件系统、
   模板规格四项同时不同，任何差值都无法归因到其中一项 —— 尤其不能归因给 HDBSS。
2. **功能正确性：950 在硬件标脏路径上 59/59 全过；920B 全套（59/59、31/31、
   `ALL CORRECT`、200 次 0 失败、pause ×2、兼容 13 OK / 2 REFUSED / 0 BROKEN）全过。**
   950 上的树语义、pause、兼容矩阵、稳定性四项**尚未跑过**。
3. **57 与 59 都写着「全部通过」。** 判据本身会静默失效，所以结果表必须逐轮标注
   SDK 与 `mem_mode` 判据是否有效 —— 这是本篇 §2 那张表存在的理由。
4. **判定结论（920B · ext4 方案 · 软件写保护 · loop ext4 · 2 GB 模板 · 链深 33）：
   按 p50，六档 checkpoint 与两种 restore 全部达标。**
   并列的 p99 在 256 MB 这一最高档越线三处（checkpoint p99 218.5 ms、
   restore 浅集 p99 118.7 ms、深集 p50 116.6 ms，深集 n=4）。
5. **256 MB 档的 p50 只剩 3.8 ms 余量**，而且是在没有硬件标脏的机器上测的。
   这一档是压线过，不是有余量。
6. **全量（1.511 s）单列，不参与判定**，但必须列出来 —— 它是「增量快 51 倍」这句话的分母。
7. **950 唯一那个 restore 0.100 s 不是判定值**：样本 10 次、混了四种跳法、
   现场是重建的、链深只有 3、max 已经 153 ms。
8. **跨实现的两张表只并列不作差**，而且都跑在软件写保护上；
   换到硬件标脏要**整轮重跑**，不是补一列。
9. **最明确的一处劣化是持续连打**：60 秒里 ×2.81（另一轮 ×3.22），判定为跟着代数走，
   根因之一是 `manifest.json` 里那块 O(链深) 的层列表记账；**爬升还略微超线性，
   说明不止这一处，没有继续深挖。**
10. **§8 那张缺口表是全书唯一维护的一份**，十六条里有一条不需要机器就能修：把条件标签记全。

---

## 延伸阅读

- [第 26 篇 §1.2 · 口径定义表](26-performance-methodology.md#12-口径定义表) —— 本篇每一张判定表的读法
- [第 24 篇 §3 · 测试矩阵](24-test-overview.md#3-测试矩阵) —— 本篇 §2、§3 的三态来源
- [第 25 篇 §2.4 · `mem_mode` 断言依赖哪一版 SDK](25-functional-tests.md#24-mem_mode-断言以及它依赖哪一版-sdk) —— 57 与 59 的差别
- [第 27 篇 §4.5 · 哪些列可以并排](27-cross-implementation.md#45-哪些列可以并排) —— §6 两张附表的口径
- [第 18 篇 §6 · 怎么选](18-ext4-vs-xfs.md#6-怎么选) —— §4.6 那张取舍表的决策版
- [`../scripts/dev/耗时基准结论.md`](../scripts/dev/耗时基准结论.md) —— 920B 两套对照的完整开发态记录

**下一篇**：[29 · 上机验收操作](29-acceptance-runbook.md) —— 把这一篇里写着「待测」的格子
变成有数的格子，需要在机器上敲哪几条命令。
