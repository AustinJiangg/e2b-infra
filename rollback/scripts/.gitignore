# 本机凭据：唯一来源是 benchmark/.env，这里只放一个指过去的软链接，不入库。
#   ln -s ../../benchmark/.env rollback/scripts/.env
.env

__pycache__/

# 各脚本每次运行产生的目录（客户端结果、采集的日志、生成的报告），不入库。
# 手工挑出来归档的实测报告也不入库，放在工作区 e2b-repo/rollback-reports/（与本仓库平级）。
*/runs/
dev/reports/
