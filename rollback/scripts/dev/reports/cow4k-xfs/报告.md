# checkpoint / restore 耗时基准 —— xfs 套

采于 20260824-214620，机器 hostname-tdi1m.foreman.pxe。
标脏后端 **kvm-wp**（KVM 写保护，软件标脏，**不含硬件标脏的收益**）。store 落在 `/home/j30059180/projects/e2b-repo/xfsdev/orchestrator/build/checkpoints`（xfs）。

## 一、全量 vs 增量

每个沙箱的第一次 checkpoint 没有基线可比，必然是全量；之后才是增量。
下表第一行就是全量，后面各行是不同脏页量下的增量。

| | 创建 p50 | p90 | p99 | max | 相对全量 | 逻辑体积 | du 实占 | **新增物理空间** |
|---|---|---|---|---|---|---|---|---|
| **全量快照** | **1.607 s** | 1.719 s | 1.719 s | 1.719 s | 1.0× | 2048.1 MB | 2048.1 MB | 2048.1 MB |
| 增量 脏页 0 MB | **0.117 s** | 0.129 s | 0.151 s | 0.151 s | 快 14× | 2048.1 MB | 2048.2 MB | 4.78 MB |
| 增量 脏页 32 MB | **0.157 s** | 0.180 s | 0.199 s | 0.199 s | 快 10× | 2048.1 MB | 2048.2 MB | 40.19 MB |
| 增量 脏页 256 MB | **0.295 s** | 0.320 s | 0.329 s | 0.329 s | 快 5× | 2048.1 MB | 2048.2 MB | 272.41 MB |

三个体积口径不一样，别混：

- **逻辑体积** = `du --apparent-size`，这次 checkpoint 在文件层面"看起来"多大。
  两套都是整份内存的大小，因为两套给出的都是一份完整的内存视图。
- **du 实占** = `du`，按文件的 st_blocks 算。ext4 套的差分是稀疏文件，这里就是真实增量；
  XFS 套是 reflink 克隆，共享的 extent 会在每个克隆里各算一遍，所以这一列会虚高。
- **新增物理空间** = 整档跑完 `df` 的净增量除以次数。**这一列才是盘上真正少掉的空间**，
  对两套都成立，也是唯一可以横向比较的一列。已排除预热（含必然是全量的第一次）。

## 三、restore

| 脏页档 | 浅集（回退一步）p50 | p99 | 深集（回到链根，33 步）p50 | 内容校验 |
|---|---|---|---|---|
| 0 MB | 0.0188 s | 0.0525 s | 0.0200 s | 全部一致 ✓ |
| 32 MB | 0.0215 s | 0.0637 s | 0.0267 s | 全部一致 ✓ |
| 256 MB | 0.0345 s | 0.0623 s | 0.0941 s | 全部一致 ✓ |

> restore 是把整个虚机换掉，客户端墙钟基本就等于停机时长，不用另外测冻结。

## 五、漂移自查

| 脏页档 | 前 1/3 p50 | 后 1/3 p50 | mem_mode |
|---|---|---|---|
| 0 MB | 0.0958 s | 0.1230 s | incremental |
| 32 MB | 0.1308 s | 0.1639 s | incremental |
| 256 MB | 0.2699 s | 0.3027 s | incremental |

> 每档都是一个全新沙箱，链深同样从 1 长到 33 —— 链深这个变量在档之间是配平的。
> `mem_mode` 全部应为 incremental；出现 full 说明那次退化成了全量拷贝。
